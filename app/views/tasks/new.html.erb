<div class="container">
  <%= form_for @task do |t| %>
    <%= t.label :title %>
    <%= t.text_field :title %>

    <%= t.label :details %>
    <%= t.text_field :details %>

    <%= t.submit %>
  <% end %>
</div>

<div class="container">
  <%= link_to "Back to Index", tasks_path  %>
</div>
